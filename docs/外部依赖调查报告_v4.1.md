# Movio AI v4.1 — 外部依赖确认清单调查报告

> T-G5-010 交付件 | 2026-05-08 | G3 架构师 + G5 后端联合出稿

---

## 总览：10 项外部依赖

| # | 依赖 | 类别 | 用途 | 接入难度 | 月费预估 | 优先级 |
|:--|:--|:--|:--|:--|:--|:--|
| 1 | MySQL 8.0+ | 基础设施 | 主数据库 | ⭐ | $0 (自建) | P0 |
| 2 | Redis 7.0+ | 基础设施 | 缓存/队列/SSE广播 | ⭐ | $0 (自建) | P0 |
| 3 | MinIO / S3 | 基础设施 | 对象存储 | ⭐ | $0 (自建) / $25+ | P0 |
| 4 | OpenAI API | AI 模型 | GPT-4o/DALL-E 3 | ⭐⭐ | 按量 | P0 |
| 5 | Anthropic Claude API | AI 模型 | Claude Sonnet/Haiku | ⭐⭐ | 按量 | P0 |
| 6 | Stable Diffusion | AI 模型 | 图片生成/抠图/扩图 | ⭐⭐⭐ | $0.5-20/h GPU | P0 |
| 7 | Seedance (即梦) | AI 模型 | 视频生成 | ⭐⭐⭐⭐ | 待确认 | P0 |
| 8 | 通义千问/万相 | AI 模型 | 文本/图片/视频 | ⭐⭐ | 按量 | P1 |
| 9 | 阿里云绿网 | 安全 | 图片内容审核 | ⭐⭐ | ¥0.001-0.003/图 | P1 |
| 10 | 通联支付 | 业务 | 聚合收银台 | ⭐⭐⭐ | 0.38%-0.6% 费率 | P2 |

---

## 1. MySQL 8.0+

### 用途
- 全部业务数据存储：用户、作品、订单、积分、分销、配置等 18 张核心表
- 配置中心数据源（`sys_config_item` 等 5 张配置表）
- 字符集: utf8mb4，排序规则: utf8mb4_unicode_ci

### 接入状态
- ✅ 19 个 SQL 迁移文件已就绪（`server/src/migration/`）
- ✅ DAO 层 18 个数据访问对象已编写
- ✅ 数据库设计文档完备
- ⚠️ 当前开发环境 MySQL 不可用，迁移未执行

### 迁移文件清单
```
m001_core.sql            — 核心表 (users/user_roles/config/dict)
m002_business.sql        — 业务表 (作品/任务/素材)
m003_membership.sql      — 会员/积分/分销表
m004_platform.sql        — 平台绑定/分发记录
m005_seed.sql            — 种子数据 (~250条配置)
m006_brand.sql           — 品牌表
m007_size_template.sql   — 尺码模板
m008_batch_template.sql  — 批量模板
m009_recharge.sql        — 充值记录
m010_payment.sql         — 支付记录
m011_badge.sql           — 徽章系统
m012_abuse.sql           — 滥用监测
m013_analytics.sql       — 统计数据
m014_sms_log.sql         — 短信日志
m015_email_template.sql  — 邮件模板
m016_config_seed_version.sql — 种子版本管理
m017_points.sql          — 积分原子操作
m018_distributor.sql     — 分销流水
m019_job_audit.sql       — 任务队列/审核日志
```

### 待办
- [ ] 开发环境安装 MySQL 8.0+
- [ ] 执行 `npx sequelize-cli db:create` 建库
- [ ] 执行 19 个迁移文件 `npx sequelize-cli db:migrate`
- [ ] 运行 seed-validator 校验种子数据完整性
- [ ] 生产环境配置主从复制（可选）

---

## 2. Redis 7.0+

### 用途
- **BullMQ 任务队列**: 视频生成、批量处理、数字人渲染等异步任务
- **配置缓存**: `sys_config` 读取缓存，写入时通过 Pub/Sub 失效
- **SSE 版本广播**: `config-version.service.js` 通过 Redis Pub/Sub 向所有客户端推送配置版本号
- **速率限流**: `rate-limit.middleware.js` 使用 Redis 计数器
- **Session 共享**（可选）: 多进程/多实例间共享 session

### 接入状态
- ✅ `server/src/dao/redis.js` Redis 连接管理已编写（含重连/超时/连接池）
- ✅ `rate-limit.middleware.js` 限流中间件已实现（含 Redis 降级到内存方案）
- ✅ `config-version.service.js` SSE 版本推送已实现
- ✅ `job-queue.service.js` BullMQ 队列已实现
- ⚠️ 当前开发环境 Redis 不可用

### Redis Key 命名空间
```
config:v{version}          — 配置版本号
config:{group_key}         — 分组配置缓存
rate:{userId}:{window}     — 速率计数
bull:video-gen             — 视频生成队列
bull:batch                 — 批量处理队列
bull:digital-human         — 数字人队列
```

### 待办
- [ ] 开发环境安装 Redis 7.0+
- [ ] 生产环境配置持久化 (AOF + RDB)
- [ ] 生产环境配置 maxmemory-policy: allkeys-lru
- [ ] 配置 Redis Sentinel 或 Cluster (高可用)

---

## 3. MinIO / S3 兼容对象存储

### 用途
- **文件上传存储**: 用户上传的商品图、参考图、视频素材
- **生成结果存储**: AI 生成的图片/视频/数字人渲染
- **分片上传**: 200MB+ 视频分片 5MB/片，合并后写入 MinIO

### 接入状态
- ✅ `server/src/config/storage.js` 存储配置已编写
- ✅ `server/src/middleware/upload.js` 上传中间件已实现（分片/断点续传）
- ✅ `server/src/services/storageService.js` 存储服务已实现（MinIO/S3 双后端）
- ✅ `client/components/common/AppMediaUpload.vue` 前端上传组件已实现
- ⚠️ 当前文件写入本地 `server/uploads/` 临时目录作为降级

### MinIO vs 阿里云 OSS
| 方案 | 月费 | 优势 | 劣势 |
|:--|:--|:--|:--|
| MinIO 自建 | $0 + 磁盘 | 无带宽费，开发测试友好 | 需维护 |
| 阿里云 OSS | ~$0.02/GB + 流量 | 弹性，CDN 加速 | 流量费高 |

### 待办
- [ ] 开发环境安装 MinIO 或配置 S3 endpoint
- [ ] 设置 Bucket: `movio-uploads`, `movio-results`, `movio-public`
- [ ] 配置 Bucket 生命周期策略（过期自动清理临时文件）
- [ ] 生产环境绑定 CDN 加速域名

---

## 4. OpenAI API

### 用途
- **GPT-4o**: 文案生成、脚本创作、标题优化、内容分析、翻译、提示词增强
- **GPT-4o-mini**: 轻量任务（分类、关键词提取、简单格式化）
- **DALL-E 3**: AI 图片生成（备用方案，主力用 SD）

### 接入状态
- ✅ `server/src/services/adapters/openaiAdapter.js` 已实现
  - `gpt4oInfer()` — 支持 system prompt + JSON mode
  - `gpt4oMiniInfer()` — 轻量推理，30s 超时
  - `dalle3Infer()` — 图片生成，1024x1024
- ✅ 自动降级: API Key 缺失时跳过注册，不影响其他模型
- ⚠️ API Key 未填写，模型健康状态 `unavailable`

### 定价参考 (2026)
| 模型 | 输入 | 输出 | 图片 |
|:--|:--|:--|:--|
| GPT-4o | $5/1M tokens | $15/1M tokens | N/A |
| GPT-4o-mini | $0.15/1M tokens | $0.60/1M tokens | N/A |
| DALL-E 3 | N/A | N/A | $0.04-0.12/图 |

### 申请流程
1. 注册 [platform.openai.com](https://platform.openai.com)
2. 创建 API Key → 填入 `OPENAI_API_KEY=`
3. （国内用户）配置代理 `OPENAI_BASE_URL=https://your-proxy/v1`
4. 设置费用限额防止超支

### 待办
- [ ] 申请 OpenAI API Key
- [ ] 填入 .env: `OPENAI_API_KEY=sk-xxx`
- [ ] 如需要，配置代理 `OPENAI_BASE_URL`
- [ ] 设置月费用上限

---

## 5. Anthropic Claude API

### 用途
- **Claude Sonnet 4.6**: 长文本分析、合规审核（`complianceService`）、复杂推理、脚本质量评估
- **Claude Haiku 4.5**: 快速分类、敏感词检测、简单安全审查

### 接入状态
- ✅ `server/src/services/adapters/claudeAdapter.js` 已实现
  - `claudeSonnetInfer()` — 120s 超时，max 4096 tokens
  - `claudeHaikuInfer()` — 30s 超时，max 2048 tokens
- ✅ Anthropic API 版本头 `anthropic-version: 2023-06-01`
- ⚠️ API Key 未填写

### 定价参考 (2026)
| 模型 | 输入 | 输出 |
|:--|:--|:--|
| Claude Sonnet 4.6 | $3/MTok | $15/MTok |
| Claude Haiku 4.5 | $0.25/MTok | $1.25/MTok |

### 申请流程
1. 注册 [console.anthropic.com](https://console.anthropic.com)
2. 创建 API Key → 填入 `CLAUDE_API_KEY=sk-ant-xxx`
3. （国内用户）配置代理 `CLAUDE_BASE_URL=https://your-proxy/v1`

### 待办
- [ ] 申请 Anthropic API Key
- [ ] 填入 .env: `CLAUDE_API_KEY=sk-ant-xxx`
- [ ] 如需要，配置代理 `CLAUDE_BASE_URL`

---

## 6. Stable Diffusion（本地 / Replicate）

### 用途
- **SD WebUI txt2img**: AI 图片生成（商品图、场景图、海报）
- **SD WebUI img2img**: 图片编辑（风格迁移、扩图、修复）
- **Replicate SDXL**: 云端降级方案（本地 SD 不可用时自动切换）

### 接入状态
- ✅ `server/src/services/adapters/sdAdapter.js` 已实现
  - `sdTxt2Img()` — SD WebUI API，支持 DPM++ 2M Karras 采样器
  - `sdImg2Img()` — 支持降噪强度 0.75
  - `replicateInfer()` — Replicate 云端降级，2s 轮询，60 次超时
- ✅ 双级降级: SD WebUI → Replicate → 不可用
- ⚠️ SD WebUI 需 GPU 服务器，Replicate API Key 未填写

### SD WebUI vs Replicate vs 阿里云
| 方案 | 成本 | 延迟 | 质量 |
|:--|:--|:--|:--|
| SD WebUI 自建 | GPU ~$0.5/h (A10) | 5-15s | 可控（模型自由选） |
| Replicate | ~$0.002/图 (SDXL) | 15-30s | 稳定 |
| 阿里云 PAI | ¥0.1-0.3/图 | 3-10s | 可控 |

### 硬件需求
| GPU | 显存 | 出图速度 (1024x1024/30步) | 月租 |
|:--|:--|:--|:--|
| RTX 4090 24GB | 24GB | ~3s | ~¥2500/月 |
| A10 24GB | 24GB | ~4s | ~¥2800/月 |
| RTX 3090 24GB | 24GB | ~4s | ~¥1800/月 |
| Tesla T4 16GB | 16GB | ~8s | ~¥1500/月 |

### 申请流程
- **本地**: 安装 AUTOMATIC1111/ComfyUI → 启动 `--api` → 配置 `SD_API_URL=http://xxx:7860`
- **Replicate**: 注册 [replicate.com](https://replicate.com) → 获取 Token → 填入 `REPLICATE_API_KEY=r8_xxx`

### 待办
- [ ] 确认使用方案（自建 GPU / Replicate / 阿里云 PAI）
- [ ] 如自建，部署 SD WebUI 并配置 `SD_API_URL`
- [ ] 如 Replicate，申请 API Key 并填入 .env
- [ ] 安装/导入项目所需模型: SDXL, RMBG-2.0, IC-Light-v2, ControlNet

---

## 7. Seedance（剪映/即梦 — 视频生成）

### 用途
- **视频生成**: 文本/图片生视频，Phase 1 核心 P0 功能
- **剪映生态对接**: Movio 作为剪映内容供给方，生成视频直接出现在剪映素材库
- **剪映草稿导出**: 导出 `.draft` 格式，用户在剪映中打开编辑

### 接入状态
- ⚠️ **未接入** — 尚无官方公开 API 文档
- ⚠️ `SEEDANCE_API_KEY=` 仅占位预留
- ✅ PRD 中已设计 SeedanceAdapter 架构（分层解耦，随时可接入）
- ✅ 视频服务已实现 `video.service.js`，预留了 Seedance 调用点

### 调研结论
| 路径 | 可行性 | 说明 |
|:--|:--|:--|
| **剪映开放 API** | ❓ 待确认 | 字节跳动剪映团队是否已开放 API，需联系商务或查阅开放平台文档 |
| **即梦 API / 火山引擎** | ⚠️ 可能 | 即梦视频生成能力可能通过火山引擎 AI 平台提供，需验证 |
| **剪映草稿 .draft 格式导出** | ✅ 可行 | 剪映支持 .draft 文件，Movio 可生成兼容格式 |
| **剪映插件生态** | ⚠️ 需审核 | 需通过剪映插件市场审核 |

### 备选方案
| 方案 | 提供商 | API | 价格 |
|:--|:--|:--|:--|
| Runway Gen-4 | Runway | REST API | $12-144/月 |
| Pika 3.0 | Pika Labs | REST API | $10-70/月 |
| Kling 2.5 | 快手 | REST API | ¥0.3-0.8/秒 |
| 通义万相视频 | 阿里云 | REST API | 按量计费 |
| CogVideoX | 开源 | 自部署 | GPU 成本 |

### 待办
- [ ] 联系剪映团队确认 API 开放状态
- [ ] 如剪映不可用，评估备选方案（推荐: Kling + Runway 双备）
- [ ] 确认后编写 `SeedanceAdapter.js`（已有架构预留）
- [ ] 导入 Seedance 定价数据到 `MODEL_CAPABILITIES`

---

## 8. 通义千问 / 万相

### 用途
- **通义千问 Turbo/Plus**: 中文文案生成、商品描述、脚本创作（替代/补充 GPT-4o）
- **通义万相**: 图片生成、风格迁移（替代/补充 SD）

### 接入状态
- ⚠️ `TONGYI_API_KEY=` / `QWEN_API_KEY=` 仅占位预留
- ⚠️ 尚未编写 Adapter（需 API Key 到位后开发）
- ✅ `modelDispatcher.js` 中预设了能力评分:
  - `tongyi_qwen`: { capability: 82, cost: 65, latency: 70, accuracy: 84 }
  - `tongyi_wanxiang`: { capability: 78, cost: 60, latency: 65, accuracy: 80 }

### 定价参考 (2026)
| 模型 | 输入 | 输出 |
|:--|:--|:--|
| 通义千问 Turbo | ¥0.3/1M tokens | ¥0.6/1M tokens |
| 通义千问 Plus | ¥2.0/1M tokens | ¥6.0/1M tokens |
| 通义万相图片 | N/A | ¥0.06-0.2/图 |

### 申请流程
1. 注册 [aliyun.com](https://aliyun.com)
2. 开通模型服务灵积 (DashScope)
3. 获取 API Key → 填入 `TONGYI_API_KEY=`
4. 复制 OpenAIAdapter 结构 → 编写通义 Adapter

### 待办
- [ ] 申请阿里云 DashScope API Key
- [ ] 编写 `server/src/services/adapters/qwenAdapter.js`（参考 openaiAdapter.js）
- [ ] 注册模型到 aiEngine: `tongyi-qwen-turbo`, `tongyi-qwen-plus`, `tongyi-wanxiang`

---

## 9. 阿里云绿网

### 用途
- **图片审核**: 检测色情、暴力、违禁品等风险内容
- **审核流程**: 用户提交内容 → moderation middleware → 文本审核(通义千问) → 图片审核(绿网) → 放行/拦截

### 接入状态
- ✅ `server/src/services/moderation.service.js` 已实现
  - `moderateText()` — 使用通义千问 moderation API
  - `moderateImage()` — 预留阿里云绿网调用点
- ✅ `server/src/middleware/content-moderation.middleware.js` 审核中间件已实现
- ✅ `content_audit_log` 表已设计（m019）
- ⚠️ 阿里云绿网未正式接入

### 审核策略
| 风险等级 | 文本 | 图片 | 动作 |
|:--|:--|:--|:--|
| pass | 合规 | 合规 | 放行 |
| review | 疑似 | 疑似 | 人工审核队列 |
| block | 违规 | 违规 | 422 拒绝 |

### 定价
| 服务 | 价格 |
|:--|:--|
| 文本审核 | ¥0.001/条 |
| 图片审核 | ¥0.001-0.003/张 |
| 视频审核 | ¥0.01-0.05/分钟 |

### 申请流程
1. 注册 [aliyun.com](https://aliyun.com)
2. 开通内容安全服务
3. 获取 AccessKey + Endpoint
4. 实现绿网 SDK 调用替换预留代码

### 待办
- [ ] 开通阿里云内容安全服务
- [ ] 编写 `Server/src/services/moderation/greenNetClient.js`
- [ ] 集成到 `moderation.service.js` 的 `moderateImage()`
- [ ] 配置 `CONTENT_MODERATION_ENABLED=true`

---

## 10. 通联支付聚合收银台

### 用途
- **会员充值**: 积分充值套餐付费
- **订阅付费**: 月度/年度会员续费
- **支付方式**: 微信支付 + 支付宝 + 银联云闪付（三合一）

### 接入状态
- ✅ `server/src/utils/allinpaySDK.js` RSA 签名已实现
- ✅ `server/src/config/allinpay.js` 配置定义已实现
- ✅ `server/src/services/allinpayService.js` 支付服务已实现（PLANS + 签名 + 验签）
- ✅ `server/src/controller/allinpayNotifyController.js` 支付回调已实现
- ✅ 前端充值页面 `pages/account/billing.vue` 已实现
- ⚠️ 仅完成沙箱验证，未申请正式商户号

### 费率
| 支付方式 | 费率 |
|:--|:--|
| 微信支付 | 0.38% |
| 支付宝 | 0.38% |
| 云闪付 (≥1000元) | 0.6% |

### 申请流程
1. 营业执照 → 通联支付开放平台入驻
2. 获取 `CUSID` (商户号) + `APPID` (应用ID)
3. 生成 RSA 密钥对 → 上传公钥 → 获取通联公钥
4. 填入 .env + 上传证书文件到 `server/certs/`

### 待办
- [ ] 准备营业执照
- [ ] 通联支付商户入驻
- [ ] 配置 .env 支付参数
- [ ] 上传 RSA 证书到 server/certs/
- [ ] 后端: 沙箱验证 → 生产切换
- [ ] 前端: 支付页面真实测试

---

## 附录 A: 平台 OAuth 清单（Phase 2，25 个平台）

| # | 平台 | OAuth 文档 | 申请门槛 | 接入难度 |
|:--|:--|:--|:--|:--|
| 1 | 淘宝 | open.taobao.com | 企业+商品 | ⭐⭐⭐ |
| 2 | 京东 | open.jd.com | 企业 | ⭐⭐⭐ |
| 3 | 拼多多 | open.pinduoduo.com | 企业 | ⭐⭐⭐ |
| 4 | 抖音 | open.douyin.com | 企业/个人 | ⭐⭐ |
| 5 | 快手 | open.kuaishou.com | 企业/个人 | ⭐⭐ |
| 6 | B站 | openhome.bilibili.com | 个人 | ⭐ |
| 7 | 小红书 | open.xiaohongshu.com | 企业 | ⭐⭐⭐ |
| 8 | 微信视频号 | developers.weixin.qq.com | 企业 | ⭐⭐⭐ |
| 9 | Shopee | open.shopee.com | 企业 | ⭐⭐ |
| 10 | Lazada | open.lazada.com | 企业 | ⭐⭐ |
| 11-25 | TikTok / YouTube / Instagram / Facebook / Twitter / Pinterest / Amazon / eBay / Wish / Mercado Libre / Tokopedia / Zalora / Bukalapak / Shopify / WooCommerce | — | — | ⭐⭐-⭐⭐⭐⭐ |

> **建议**: Phase 1 先完成抖音+快手+B站 3 个平台，验证分发链路；Phase 2 扩展至 25 平台。OAuth 申请通常需要 1-4 周审核周期。

---

## 附录 B: SMS 短信服务

| 服务商 | 单价 (验证码) | 接入难度 | 当前状态 |
|:--|:--|:--|:--|
| 阿里云短信 | ¥0.045/条 | ⭐ | 未配置 |
| 腾讯云短信 | ¥0.045/条 | ⭐ | 未配置 |

---

## 附录 C: Email 邮件服务

| 服务商 | 免费额度 | 接入难度 | 当前状态 |
|:--|:--|:--|:--|
| SMTP (QQ/Gmail/163) | 免费 | ⭐ | 代码已实现，SMTP 配置未填 |
| SendGrid | 100封/天 | ⭐⭐ | 代码已预留 |

---

## 行动建议

### 立即执行（本周）
1. 安装 MySQL 8.0 + Redis 7.0 + MinIO（或用 Docker 一键启动 `docker-compose up -d`）
2. 申请 OpenAI API Key（GPT-4o-mini 即可开始开发）
3. 申请 Anthropic API Key（非紧急，合规功能可暂用 GPT 替代）

### 本月
4. 确认 Seedance/剪映 API 状态（联系字节商务）
5. 如剪映不可用，备选方案评估（推荐 Kling + Runway）
6. 申请阿里云 DashScope（通义千问 + 万相）
7. 通联支付商户入驻启动

### Phase 2
8. 平台 OAuth 逐步接入（抖音→快手→B站→淘宝→...）

---

**文档版本**: v1.0 | **下次审核**: 2026-05-22 | **依据**: `PRD_v4.1_最终定版_Movio_AI.md`
